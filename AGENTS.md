# TikHub 短剧报表前端开发约束

本文件是本项目的强制开发护栏。修改公开前端、后台管理面板、报表缓存、页面导航或数据接口前必须先阅读；如果实现方案与本文件冲突，应先更新设计并说明原因，禁止无意中恢复旧行为。

## 1. 页面和路径边界

- `/` 是用户公开前端，包含四个站内页面：运营看板 → 公司短剧榜单 → 监控账号 → 报表。
- 四个公开页面必须通过 `setPublicPageView()` 在原页面内切换，地址栏保持原域名根路径；不得把“公司短剧榜单”重新改成跳转 `/catalog` 的普通链接。
- 公司短剧榜单通过 `#catalogFrame` 懒加载 `/catalog?embed=1`，嵌入模式必须隐藏榜单自身顶栏，避免出现双重导航。
- `/catalog` 只保留为兼容旧书签的独立入口，不作为公开前端的常规导航方式。
- `/admin` 是唯一独立的后台管理路径。公开用户页面不得出现后台密码、任务密码或后台管理操作。

## 2. 公开前端双文件必须完全同步

- `index.html` 与 `tikhub-report-frontend.html` 是同一公开前端的两个部署副本，任何功能、样式、缓存或导航修改必须同时应用。
- 提交前必须确认两个文件 SHA-256 完全一致；禁止只改其中一个文件。
- 自动测试必须继续覆盖页面切换、缓存版本、历史加载和双文件约束。

## 3. 数据源优先级

- 用户前端最新数据：优先 `/supabase/latest`，历史列表使用 `/supabase/reports`，单份历史使用 `/supabase/report`。
- `public_reports/latest_report.json` 和静态 manifest 仅是 Supabase 不可用时的备用快照，不得当作线上“最新数据”。
- 后台管理面板未验证密码时也必须先读取 `/supabase/latest`，失败后才能回退静态快照；不能再次默认显示仓库里旧的 12/13 个账号。
- 后台验证密码后，监控账号总数以 `/schedule-accounts` 返回的完整后端监控池为准；账号卡片使用“最新报表账号 ∪ 后端监控池”的并集，未成功抓取的监控账号仍要显示并标记为需要检查。
- 最新报表账号数和监控池账号数含义不同：前者是本次抓取结果，后者是定时任务配置。界面必须明确标注来源，不得混用。

## 4. 刷新、缓存和历史数据

- 最新完整报表约 5 MB，不能存入 `localStorage`。大报表和近 10 次历史必须使用 Cache Storage（当前缓存名 `tikhub-dashboard-cache-v3`）。
- `localStorage` 只保存小型设置和短时校验信息；写入失败时内存回退必须可被后续读取。
- 页面刷新时可立即展示最近两分钟内已确认版本的完整缓存，同时后台静默校验最新元数据；超过校验窗口必须先确认版本，防止旧 13 个账号闪现后再跳到新数据。
- 如果最新报表缓存存在但历史缓存尚未完成，不能把涨幅、近 30 天榜单、本月题材增长显示成“暂无”。必须显示“正在加载历史对比数据”，完整历史准备好后一次性展示。
- 近 10 次历史只在最新报表变化或本机历史缓存缺失时重新下载；已有完整缓存时不得每次刷新重复下载全部历史报表。
- 历史下载当前并发为 4。调整并发前必须评估浏览器内存、Render 512 MB 内存和接口响应体积，并通过实际加载测试。
- 自动检查周期保持 60 秒；连续手动刷新不能不断重启大文件下载并导致页面长期不完成。

## 5. 数据展示完整性

- 公开运营看板必须保留：当前最高热度、近 7/3/1 天涨幅、近 30 天热度新增榜、热门题材占比、本月题材播放增长、报表账号数、本周新增短剧和增长最多账号。
- 历史基准确实不存在时才能显示“暂无历史基准”；网络请求尚未完成时必须显示加载状态。
- “最新”排序可能没有日增/周增数据，“最热”排序可能有数据，这是数据基准差异，不得用伪造数值填充。
- 后台账号页必须区分“抓取正常”和“需要检查”；后端池中存在但最新报表没有数据的账号不能被过滤掉。

## 6. 鉴权和安全

- 后端密码只允许通过 `X-Schedule-Secret` 请求头发送；禁止恢复 `?secret=` 查询参数兼容。
- 下载、播放源、代理、后台写入和监控池管理必须继续执行现有请求头鉴权。
- 公开页面和公开接口不得泄露 `SCHEDULE_SECRET`、Supabase Service Key 或 TikHub API Key。

## 7. 每次前端改动的交付检查

1. 运行 `python -m unittest discover -s tests -v`，所有测试通过。
2. 运行 `python -m py_compile tikhub_proxy.py`。
3. 确认 `index.html` 和 `tikhub-report-frontend.html` 哈希一致。
4. 运行 `git diff --check`，不得有冲突标记或空白错误。
5. 浏览器验证：首页完整历史模块、榜单站内切页、监控账号页、报表页和 `/admin`。
6. 验证连续刷新不会回退到旧账号数，不会短暂把历史数据显示为“暂无”。
7. 验证后台未登录时读取线上最新报表，登录后显示完整后端账号池。
8. 用户已要求开发完成后自动推送 `main`；推送后必须等待 Render 部署并再次验证线上版本。

## 8. 禁止回退清单

- 禁止把公司短剧榜单改回新域名或独立跳转。
- 禁止只修改一个公开前端文件。
- 禁止用静态 `public_reports` 覆盖 Supabase 最新数据。
- 禁止把 5 MB 级报表重新写入 `localStorage`。
- 禁止为了“快速显示”先渲染不完整历史并显示“暂无”。
- 禁止每次刷新重新下载全部 10 份历史报表。
- 禁止把最新报表账号数当作完整监控池账号数。
- 禁止恢复查询参数密码。
